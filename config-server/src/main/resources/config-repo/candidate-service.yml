spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agenthire_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: agenthire
    password: agenthire123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  redis:
    host: localhost
    port: 6379
    password: redis123

file:
  upload-dir: ./uploads/resumes

server:
  port: 8082
